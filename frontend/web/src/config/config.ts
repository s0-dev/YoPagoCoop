// Aca se van a configurar
// - Sitio
// - Idiomas
// - Temas

const siteConfig = {
  // seo
}

const i18nConfig = {
  // idiomas
}

const themeConfig = {
  // temas [dark, light, auto]
}

const config = {
  site: siteConfig,
  i18n: i18nConfig,
  theme: themeConfig,
}

export default config;